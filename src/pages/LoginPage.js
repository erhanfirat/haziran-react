import PageDefault from "./PageDefault";
import LoginForm from "../components/LoginForm";

const LoginPage = () => {
  return (
    <PageDefault pageTitle={"Login Page"}>
      <LoginForm />
    </PageDefault>
  );
};

export default LoginPage;
